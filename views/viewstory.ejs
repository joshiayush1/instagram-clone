<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/viewstory.css">
</head>
<body>
    <div class="cont">
        <p><%= user.username %></p>
        <div class="story">
            <% user.story.forEach(function(story) { %>
                <img src="/images/uploads/<%= story %>" alt="Story Image" class="rounded-lg">
            <% }) %>
        </div>
    </div>
    <script src="https://cdn.tailwindcss.com"></script>
</body>
</html>